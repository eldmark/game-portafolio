# Runtime Assets

Place production assets here before publishing:

- `audio/cozy-loop-placeholder.mp3`
- project GIF demos in `gifs/`
- compressed textures in `textures/`
- optimized low-poly models in `models/`

The current app uses geometry placeholders so it can run before custom art is authored.
